logging:
  level:
    org:
      springframework:
        security: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quill
    username: postgres
    password: postgres

  jpa:
    show-sql: true